# Frontend

This directory is reserved for the future React frontend.

Suggested bootstrap (when ready):

```sh
npm create vite@latest . -- --template react-ts
npm install
npm run dev
```

Then configure the API base URL to call the Rust backend.
